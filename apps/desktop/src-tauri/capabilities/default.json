{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Default window: the React lens talks to vistalithd over HTTP; shell commands only expose backend health/start.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
