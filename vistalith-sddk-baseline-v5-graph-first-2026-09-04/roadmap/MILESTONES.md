# Milestones

## M0 — SDDK really is the core
Vistalith process opens project and calls SDDK directly.

## M1 — One semantic world
At least 50 mixed SDDK/code/visual/chat subjects share stable identities and
queryable relations.

## M2 — Event replay
Delete derived SWG and rebuild to equivalent graph hash.

## M3 — Semantic chat
`@PaymentService` context explains why each item was included.

## M4 — Graph-reactive behavior
A code-change observation triggers a deterministic relation behavior that creates
an advisory impact proposal, with trace.

## M5 — Draw → semantic proposal
Canvas sketch promotes to VisualIntent without touching files.

## M6 — Architecture ↔ workflow
Same SubjectRef is revealed in C4 and direct SDDK workflow projection.

## M7 — Governed promotion
Visual proposal creates governed SDDK work through direct core calls.

## M8 — Fork/diff
Explore two alternatives and structurally compare graph/evidence/cost.

## M9 — Why
Navigate architecture subject → decision → requirement → evidence.

## M10 — Upstream learning
At least one Vistalith innovation passes pull-up criteria and is submitted as an
SDDK proposal without importing Vistalith mechanics.
