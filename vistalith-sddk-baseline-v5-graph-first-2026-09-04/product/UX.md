# UX

```text
┌──────────────────────────────────────────────────────────────────┐
│ Project │ WorkItem │ Branch/Revision │ Model Profile │ Budget    │
├───────────────┬──────────────────────────────┬───────────────────┤
│ Navigator     │ Main Workspace               │ Inspector         │
│               │                              │                   │
│ Threads       │ Chat / C4 / Canvas / Graph   │ Subject           │
│ Subjects      │ Workflow / Decision / Impact │ Why               │
│ Agents        │                              │ Evidence          │
│ WorkItems     │                              │ Provenance        │
│               │                              │ Relations         │
├───────────────┴──────────────────────────────┴───────────────────┤
│ Timeline │ Trace │ Usage │ Diff │ Tests │ Terminal │ Events      │
└──────────────────────────────────────────────────────────────────┘
```

## Graph-native UX
Any subject can open:
- neighbors;
- path to evidence;
- path to code;
- path to decision;
- impact;
- history;
- provenance.

## Chat-native UX
`@SubjectRef` mentions inject semantic context rather than only file content.
